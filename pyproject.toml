[project]
name = "flowkit"
version = "0.1.0"
description = "Windows desktop toolkit — launcher, flow orchestrator, and monitoring dashboard"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.31.0",
    "pyyaml>=6.0",
    "pywebview>=5.0",
]

[tool.uv]
dev-dependencies = []
