[project]
name = "monitor"
version = "0.1.0"
description = "Desktop monitoring dashboard with pluggable cards"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.31.0",
    "pyyaml>=6.0",
]

[tool.uv]
dev-dependencies = []
